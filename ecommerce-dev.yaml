token:
  expiration_time: 86400000
  secret: user_token_dev

gateway:
  ip: 192.168.0.8

order_service:
  url: http://ORDER-SERVICE/order-service/%s/orders
  exception:
  orders_is_empty: User's orders is empty.

# 바뀐 설정을 스프링에 적용법
# 1. 서버 재기동
# 2. Acutator refresh
# 3. spring cloud bus사용